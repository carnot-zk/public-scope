/** Re-export canonical network + program IDs from `@carnot/sdk` (single source of truth). */
export {
  PROGRAM_IDS,
  normalizeSolanaNetwork,
} from "@carnot/sdk";
export type { ProgramIds, SolanaNetwork } from "@carnot/sdk";
